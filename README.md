# About

## Application API

The application only can be used when your browser is at link: [localhost](‘http://localhost:3000’). If you try to make a request to the API from any other address, the request will fail.